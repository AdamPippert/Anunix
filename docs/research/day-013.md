# Day 013: Validate candidate tuning policies

The July 8 briefing describes a control plane that proposes typed policies for deterministic runtime enforcement.
It separates candidate verification, measurement, promotion, and rollback.
Anunix already has Resource Twin snapshots and candidate routing weights.
The comparison baseline, `242fe58`, checks only for zero divisors before simulation.
Negative divisors invert resource costs, and extreme weights can overflow scoring arithmetic.
An unchecked snapshot count can also exceed its fixed engine array.

## Scope

The source is [Day 013 in the daily review](daily-review.md#day-013--2026-07-08).
The source message identifier is `5048a664-da01-470d-a8f3-841629df80a5`.
This step validates the existing typed simulation inputs before evaluating a candidate.

## Implementation

The policy validator accepts bonuses from 0 through 1000 and penalties from -1000 through 0.
Cost divisors must be between 1 and 1000.
These limits are project guardrails, not experimentally selected tuning values.

Snapshot validation checks the engine count, class, status, derived readiness, and topology ranges.
Resource weights and quality scores must follow their declared 0 through 100 range.
The simulator also validates the cell's routing strategy, locality, and declared topology range.
These bounds keep scores and margins within signed 32-bit arithmetic.

All validation precedes output initialization.
A rejected candidate leaves the prior simulation result unchanged.
Simulation does not modify or activate the incumbent policy.

The [native regression](../../kernel/core/tools/research_day013.c) rejects 20 malformed inputs.
It checks result preservation and verifies that each rejected policy remains unchanged.
A valid alternate policy changes the simulated winner.
Replaying the incumbent then reproduces the original result.

## Validation

The baseline guest at `981d5b9` reports `RESEARCH day-013 FAIL rc=-1301`.
The candidate at `a199e97` passes all 59 host suites and the conformance threshold gate.
Both kernel and UEFI boots on Jekyll pass Days 001 through 012, followed by two Day 013 invocations.

| Check | Evidence | Result |
| --- | --- | --- |
| Baseline check | [Baseline result](evidence/day-013/baseline-failure.json) | Expected failure: `-1301` |
| Host suites | [Host output](evidence/day-013/host-tests.log) | 59 passing suites |
| Kernel boot | [Kernel result](evidence/day-013/kernel-boot.json) | 14 native checks pass |
| UEFI image boot | [Image result](evidence/day-013/uefi-boot.json) | 14 native checks pass |
| Guest output | [Serial log](evidence/day-013/uefi-serial.log) | Boot and regression output |

## Limits

This change validates a simulation API; it does not implement live kernel tuning or policy activation.
The live planner still uses its existing constants.
No Linux tuning mechanism, policy compiler, signed revision, or live rollback protocol is added.
The fixture uses synthetic engines and establishes deterministic validation behavior, not workload performance gains.
The guardrails constrain valid candidates; they do not establish that a candidate improves an operating workload.

## Reproduction

The [Day 001 commands](day-001.md#reproduction) build the kernel and UEFI image.
Run `tools/research_vm.py` with `--image build/anunix-x86_64-uefi.img`, `--test day-013`, `--revision a199e97`, and a new `--out-dir`.
